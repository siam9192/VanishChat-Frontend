export interface IAvatar {
  id: number;
  url: string;
  status: string[];
  createdAt: string;
  updatedAt: string;
}

export interface IRoomPhoto {
  id: string;
  url: string;
  status: string[];
  createdAt: string;
  updatedAt: string;
}
